{
  "name": "hk-recorder",
  "version": "1.0.0",
  "description": "Headless screen recording engine (Electron desktopCapturer + MediaRecorder, chunked segments, ffmpeg stitching)",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron --ozone-platform=x11 --disable-features=WebRtcPipeWireCapturer .",
    "test-record": "electron --ozone-platform=x11 --disable-features=WebRtcPipeWireCapturer . --test-record"
  },
  "license": "MIT",
  "dependencies": {
    "ffmpeg-static": "^5.2.0"
  },
  "devDependencies": {
    "electron": "^31.3.1"
  }
}
